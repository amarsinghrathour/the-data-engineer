# Resources

Curated links, reading lists, cheat sheets, and official documentation pointers.

Keep entries short: title, URL, one sentence on when to use it.

## Local resources

- `resources/datasets/` — reusable JSON/CSV fixtures shared across SQL, ETL, Spark, Kafka, and data-quality notebooks.
- `resources/scripts/generate_data.py` — synthetic data generator for larger local test sets.
