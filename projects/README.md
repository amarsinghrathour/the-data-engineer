# Projects

Graded or portfolio projects live here.

Current projects:

- **`batch_pipeline/log_analyzer/`** — Phase 0 CLI log analyzer (see project `README.md`)
- **`batch_pipeline/etl_pipeline.py`** — HTTP → Postgres ETL used by Airflow notebook `notebooks/05_airflow/04_pipeline_integration.ipynb`

Suggested layout as you add phases:

```text
projects/
├── batch_pipeline/
├── phase2_mini_warehouse/
├── ...
└── capstone/
```

Each project folder should include its own `README.md` (problem statement, run instructions, assumptions).
